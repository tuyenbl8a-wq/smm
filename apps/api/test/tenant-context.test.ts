import test from "node:test"; import assert from "node:assert/strict"; import {normalizeHostname,TenantResolver,TenantError,ROOT_SITE_ID} from "../src/tenant/context.js";
test("normalizes DNS host and strips port",()=>assert.equal(normalizeHostname("Panel.Example.COM:443"),"panel.example.com"));
test("rejects malformed host",()=>{for(const host of ["javascript:alert(1)","bad_host.test","a..test","user@example.com"])assert.throws(()=>normalizeHostname(host),/Invalid hostname/)});
test("unknown and unverified domains fail closed",async()=>{const resolver=new TenantResolver({site:{findUniqueOrThrow:async()=>({id:ROOT_SITE_ID})},siteDomain:{findFirst:async()=>null}},new Set(["dichvu1st.com"]));await assert.rejects(()=>resolver.resolve({headers:{host:"unknown.test"}}),/Unknown or unverified/);assert.equal((await resolver.resolve({headers:{host:"dichvu1st.com"}})).id,ROOT_SITE_ID)});
test("suspended ancestor blocks descendant",async()=>{const sites:any={leaf:{id:"leaf",parentSiteId:"parent",status:"ACTIVE",depth:2},parent:{id:"parent",parentSiteId:ROOT_SITE_ID,status:"SUSPENDED",depth:1}};const resolver=new TenantResolver({site:{findUnique:async({where}:any)=>sites[where.id]}},new Set());await assert.rejects(()=>resolver.assertOperational(sites.leaf),/ancestor/)});
