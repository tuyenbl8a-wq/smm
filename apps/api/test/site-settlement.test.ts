import test from "node:test";import assert from "node:assert/strict";import {SiteSettlementService} from "../src/order/site-settlement.js";
const service={id:"svc",rate:"1.00000000"};
for(const depth of [1,2,3])test(`${depth+1}-level hierarchy quotes every upstream edge`,async()=>{const sites:any={root:{id:"root",parentSiteId:null,ownerUserId:null}};let parent="root";for(let i=1;i<=depth;i++){sites[`s${i}`]={id:`s${i}`,parentSiteId:parent,ownerUserId:`owner${i}`};parent=`s${i}`}const tx={site:{findUnique:async({where}:any)=>sites[where.id]},siteServiceRule:{findUnique:async()=>({active:true,pricingMode:"COST_PLUS_FIXED",fixedProfit:"1.00000000",minProfit:"0"})}};const edges=await new SiteSettlementService({}).quote(tx,parent,service,1000);assert.equal(edges.length,depth);assert.deepEqual(edges.map(x=>x.payerUserId),Array.from({length:depth},(_,i)=>`owner${i+1}`))});
